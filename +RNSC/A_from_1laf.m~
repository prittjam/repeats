function [A,inl] = A_from_1laf(u,T)
inl = [];
A = [];
for k = 1:size(u,2)
    A = HG.A_from_1laf(u(:,k));
    err = LAF.sampson_err(u);
    inl = all(err < T);
    if sum(inl) > sum(best_inl)
        best_inl = inl;
        
end